# x402 Payment Configuration
# Copy this file to .env.local and fill in your values

# Enable x402 payments
X402_ENABLED=true

# x402 Facilitator URL (use default for development)
X402_FACILITATOR_URL=https://x402.org/facilitator

# Network for payments (base, baseSepolia, ethereum, polygon)
X402_NETWORK=baseSepolia

# USDC Contract Address (auto-detected based on network)
X402_USDC_CONTRACT=0x036CbD53842c5426634e7929541eC2318f3dCF7e

# Payment amount in smallest unit (e.g., "10000" = 0.01 USDC with 6 decimals)
X402_QUERY_PRICE=10000

# Force payments for testing (set to true for development)
X402_FORCE_PAYMENT=true

# Skip validation for development (set to true to avoid facilitator issues)
X402_SKIP_VALIDATION=true

# Agent private key (required for signing payments)
# The wallet address will be derived from this if X402_WALLET_ADDRESS is not set
AGENT_PRIVATE_KEY=0xYourPrivateKeyHere

# Optional: Set wallet address explicitly (will be derived from private key if not set)
# X402_WALLET_ADDRESS=0xYourWalletAddressHere

# The Graph API Configuration (if using paid endpoints)
# GRAPH_API_KEY=your_graph_api_key_here
# NEXT_PUBLIC_GRAPH_TOKEN=your_jwt_token_here
# NEXT_PUBLIC_GRAPH_API_KEY=your_graph_api_key_here 